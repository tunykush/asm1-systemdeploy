{
  "name": "notes",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test:lint": "eslint .",
    "test:unit": "jest --ci tests/unit --coverage --forceExit",
    "test:integration": "jest --ci tests/integration --coverage --forceExit",
    "test:e2e": "npx playwright test",
     "build": "mkdir -p dist && find . -maxdepth 1 ! -name dist ! -name node_modules ! -name .git -exec cp -r {} dist/ \\; && rm -rf dist/tests dist/*.log dist/.git*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "ejs": "^3.1.6",
    "enoent": "^1.0.0",
    "express": "^4.17.3",
    "method-override": "^3.0.0",
    "mongoose": "^6.2.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "eslint": "^8.11.0",
    "expect.js": "^0.3.1",
    "jest": "^27.5.1",
    "jest-junit": "^13.0.0",
    "mockingoose": "^2.15.2",
    "supertest": "^6.2.2"
  }
}